import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h1" color="primary.contrastText">
        Weather App
      </Typography>
    </div>
  );
};

export default Home;
